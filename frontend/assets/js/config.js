// config.js — Mychat v9
'use strict';

const CONFIG = {
  // Change this to your Render backend URL after deploy
  API_BASE: 'https://mychat-v9-backend-lr1c.onrender.com',
  WS_URL: 'wss://mychat-v9-backend-lr1c.onrender.com',
  PERMANENT_ID_MIN: 3,
  PERMANENT_ID_MAX: 32,
};

window.CONFIG = CONFIG;
